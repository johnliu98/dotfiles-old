#!/bin/sh

# The clickable help menu. Middle click to restart wm.

case $BUTTON in
	1) notify-send "❓ Help module" "\- Shift click to open script." ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac; printf "❓"
